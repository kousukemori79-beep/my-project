# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is currently a fresh, essentially empty project. As of this writing it contains only:

- `README.md` — a single-line placeholder (`# my-project`)
- This `CLAUDE.md`

There is **no source code, build system, dependency manifest, test suite, or CI configuration yet**. No language or framework has been chosen. There are no Cursor (`.cursor/rules/`, `.cursorrules`) or Copilot (`.github/copilot-instructions.md`) rule files.

Because nothing has been scaffolded, there are no build, lint, run, or test commands to document yet. **Update this file as soon as a stack is chosen** — add the real commands (build, lint, run, test, and how to run a single test) and a description of the architecture once code exists. Do not invent commands that aren't backed by actual project files.

## Repository

- GitHub: `kousukemori79-beep/my-project`
- Default branch: `main`
